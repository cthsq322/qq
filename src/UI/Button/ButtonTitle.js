import React from "react";

import classes from "./ButtonTitle.module.css";

const ButtonTitle = () => {
    return <button className={classes.button}>Lens 101</button>;
};

export default ButtonTitle;
