import React from "react";

import classes from "./Button.module.css";

const Button = () => {
    return <button className={classes.button}>Log In /Sing Up</button>;
};

export default Button;
